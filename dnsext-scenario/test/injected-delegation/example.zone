; The zone a resolver is asking about when the injection arrives.  Its
; own data is unremarkable; what it sends along with it is not.
$ORIGIN example.
$TTL 3600
@	IN	SOA	ns.example. root.example. 1 3600 900 604800 3600
@	IN	NS	ns.example.
ns	IN	{{AUTH_ADDR_RR}}
www	IN	A	192.0.2.1
