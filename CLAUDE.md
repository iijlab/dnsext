# dnsext

## 言語

- PR のタイトルと説明、issue、レビューコメントは日本語で書く。
- コミットメッセージの 1 行目（件名）は英語で書く。先頭には、これまでどおり対象のパッケージ名を付ける（例：`dnsext-iterative: ...`）。
- 2 行目は空行にし、3 行目以降の本文（経緯、理由、詳細）は日本語で書く。本文が要らない変更なら、件名だけでよい。
- コード中の識別子は英語。コメントと Haddock は、そのファイルの既存の書き方に合わせる。

## 履歴の形

- main の履歴は、「マージコミットで閉じた輪」が一直線につながる形に保つ。
- PR をマージする前に、ブランチを最新の main の上に rebase する。マージは、fast-forward せずにマージコミットを作る。
- 複数の PR を続けてマージするときは、1 本マージするたびに、次のブランチを rebase し直す。

## PR の作り方（レビューする人の負担を減らす）

- PR の説明の冒頭には、ひと目で全体が分かる 3 行以内の概要を書く。
- その後に続ける内容は次の 4 点だけにし、箇条書きを基本にする。長い物語調の説明は書かない。
  - 何を変えたか
  - なぜ変えたか
  - どの層・どのパッケージに影響するか
  - どう確かめたか（テスト、実測）
- 実測の結果やログは、判断に必要な部分だけ載せる。
- 1 つの PR では 1 つの話題だけを扱う。
- 複数の PR にまたがる作業は、PR を作り始める前に、PR の一覧と順番、それぞれの要点を示して確認を取る。一度に大量の PR を作らない。

## 構成

- `dnsext-do53`：クライアントから使うスタブリゾルバのライブラリ。フォールバックや再送の仕組みを持つために、反復検索サーバから制御できないループの危険が大きく、部品としては利用できない。
- `dnsext-iterative`：反復検索（bowline が使う）。権威サーバへの問い合わせは、上から順に次の層を通る。
  1. `DNS.Iterative.Query.ResolveJust`：委任をたどり、どの NS に何を問い合わせるかを決める。**Norec を呼ぶ層。**
  2. `DNS.Iterative.Query.Norec`：1 つの問い合わせを、候補のアドレスに投げる。
  3. `DNS.Iterative.Query.Do53Stub`：UDP/TCP で 1 回送って 1 回受け取る（`OneshotResolver`）。

## 設計の原則：Norec 以下は「1 回送って 1 回受け取る」だけ

Do53Stub は、権威への問い合わせからループを無くすために単純化した層である（`8c4b88fe`「simplified *Resolver1, no loop resolver, no retry loop, no NoEDNS fallback loop」）。

Norec と Do53Stub では、次のことをしない。

- クエリの書き換え（名前、フラグ、EDNS など）。渡されたクエリをそのまま送る。
- 応答を見て問い合わせ直すこと（再送、フォールバック）。
- 応答を待つループ。応答がおかしければ、すぐに失敗を返す。

例外：TCP フォールバック

- TC ビットを立てた権威サーバと同じサーバに聞き直す目的で、Do53Stub の中での TCP フォールバックを許している。上の層では、どのサーバが TC を立てたかが分からないためである。
- 送るのは同じクエリで、送り先も同じサーバ、回数は 1 回だけ。TCP で受け取った応答をきっかけに、もう一度送り直すことはしない。
- TCP に切り替える条件は、上の層から渡される判定関数で決める（TC のほかに、例えば DNS-0x20 で表記が合わなかった場合）。Do53Stub は、その条件が何を意味するかを知らない。

問い合わせの失敗を補うのは、すべて Norec を呼ぶ層（`ResolveJust`）の役目である。

- 別の NS に問い合わせる、クエリを変えて問い合わせ直す、といった判断はここだけで行う。
- 回数の上限もここで持つ。無制限にループしうる作りにしない。
- ポートや ID を知った攻撃者に対しても、失敗を早く返して上位で問い合わせ直す方が安全である。新しいソケットと新しい ID で送り直すことになり、攻撃者が 1 回の問い合わせで試せる回数が 1 回に限られる。

応答の照合（ID、Question の一致）は Do53Stub の `checkResp` で行う。照合の方針を変えるときも、影響は `checkResp` の中に閉じ込め、Do53Stub が上位の方針（例：名前の大文字小文字を混ぜているかどうか）を知らずに済むようにする。

## 既知の逸脱（別件で対処予定。前例として真似しない）

- `Do53Stub.udpTcpResolver1` の `caseNoEDNS`：FormErr で EDNS を外して送り直している。移植元から持ってきたもので、削除するか、上位に移す予定。
- `Do53Stub` の `recvAnswer`：照合に通らない UDP パケットを捨てて受信を続けるループがある。一方で、デコードに失敗したときはすぐ終わる。どちらも見直す予定。
- DNS-0x20（#277、#284）：case を混ぜる処理を `Norec` で、表記を変えて返すサーバへの問い合わせ直しを `Do53Stub` で行っている。巻き戻して、`ResolveJust` で作り直す予定。

## 変更するときの進め方

- 通信層やリゾルバの構造に触る変更では、実装する前に次のことを説明し、確認を取る。
  - その処理をどの層に置くか。上の原則と衝突しないか。
  - 関係するモジュールのコミット履歴と Haddock を読み、既存の設計の意図と合っているか。
- RFC を根拠にするときは、節番号と、その節に実際に書いてある内容（MUST/SHOULD か、など）を確かめてから書く。
- RFC になっていないもの（例：DNS-0x20 は draft が失効している）を、規格上の要求のように書かない。
- `dnsext-do53` の関数（`checkRespM` など）は、スタブ、DoX、clove でも使われている。反復検索のための変更を入れるときは、`dnsext-iterative` の中に閉じ込めることを優先する。
